# Decrypted databases and keys - NEVER upload
all_keys.json
wechat_key.txt
config.json
decrypted/
decoded_images/
*.db
*.db-shm
*.db-wal
*.db.tmp_monitor

# Chat export/transcription output files (contain private message data)
*_export*.json
*_transcribed*.json

# Hook outputs
hook_output.txt
hook_start_output.txt
hook_stderr.txt
run_hook.bat

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
venv/

# OS
.DS_Store
Thumbs.db

# Compiled binaries and output
find_all_keys_macos
decoded_voices/
voice_transcriptions.json

# Claude Code 工作流内部规范（不上传公共 fork）
CLAUDE.md
.claude/
docs/internal/

# 本地机器配置 (含 venv 绝对路径，机器相关)
.mcp.json

# 个人应用脚本 (非工具集职责，作为 wxdec 的下游消费者)
scripts/watch_messages.py
